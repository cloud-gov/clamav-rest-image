---
applications:
- name: clamav-rest
  instances: 1
  memory: 2G
  docker:
    image: ((ecr_repository)):((image_version))
    username: ((docker_username))
  env: 
    CF_DOCKER_MAX_SCAN_SIZE: 100M 
    CF_DOCKER_MAX_FILE_SIZE: 100M
  routes:
  - route: clamav-rest.apps.internal
