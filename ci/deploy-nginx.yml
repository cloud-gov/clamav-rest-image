platform: linux
inputs:
  - name: source

params:
  CF_API: 
  CF_ORG:
  CF_PASSWORD:
  CF_SPACE:
  CF_USERNAME:
  CONCOURSE_GARDEN_IP_RANGE1:
  CONCOURSE_GARDEN_IP_RANGE2:

run:
  path: source/ci/deploy-nginx.sh