platform: linux

inputs:
  - name: clamav-rest-release
  - name: golang-release
  - name: source

params:
  CF_API: 
  CF_ORG:
  CF_PASSWORD:
  CF_SPACE:
  CF_USERNAME:
  CLAMAV_REST_HOSTNAME:

run:
  path: source/ci/update-versions.sh